// LooseScreen.tsx
import { FC } from 'react';

interface LooseScreenProps {
    // You can add more props if needed
}

const LooseScreen: FC<LooseScreenProps> = () => {
    return (
        <div>
            {/* Add your loose screen content here */}
            <h1>You Loose!</h1>
        </div>
    );
};

export default LooseScreen;
