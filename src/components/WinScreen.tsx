// WinScreen.tsx
import { FC } from 'react';

interface WinScreenProps {
    // You can add more props if needed
}

const WinScreen: FC<WinScreenProps> = () => {
    return (
        <div>
            {/* Add your win screen content here */}
            <h1>You Win!</h1>
        </div>
    );
};

export default WinScreen;
